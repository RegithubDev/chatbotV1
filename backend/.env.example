# ===== Recollect AI Bot =====
# Source of truth = this live DB. Never invent schema.

DB_TYPE=mysql
DB_HOST=localhost
DB_PORT=3306
DB_NAME=aakridb
DB_USER=aakri_readonly
DB_PASSWORD=CHANGE_ME
DB_SSL=false

# hard safety
DB_STATEMENT_TIMEOUT_MS=20000
DB_MAX_ROWS=200
DB_READ_ONLY=true

# LLM only for: retrieve-hint, SQL draft, answer wording
LLM_PROVIDER=openai
OPENAI_API_KEY=sk-REPLACE
OPENAI_MODEL=gpt-4.1
OPENAI_EMBED_MODEL=text-embedding-3-small

# API
PORT=3001
CORS_ORIGIN=http://localhost:5173
